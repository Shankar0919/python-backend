{
  "name": "python-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "nx": "nx",
    "lint": "nx lint python-backend",
    "test": "nx test python-backend",
    "serve": "nx serve python-backend",
    "generate-apispec": "nx generate-apispec python-backend"
  },
  "devDependencies": {
    "nx": "^20.0.0"
  }
}
